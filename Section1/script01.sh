#!/bin/bash

# This script is used to create a new directory and copy files from the current directory to the new directory.
# It also creates a log file to record the actions taken by the script.
# Usage: ./script01.sh <new_directory_name>
# Example: ./script01.sh my_new_directory
# Check if the user provided a directory name